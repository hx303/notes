# The sealed runner shares this container's network namespace. Trust only its
# loopback TCP connection; reject Unix sockets and every non-loopback address.
local all all reject
host all all 127.0.0.1/32 trust
host all all ::1/128 trust
host all all 0.0.0.0/0 reject
host all all ::0/0 reject
