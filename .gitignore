.DS_Store
.gitignore
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix
# Don't track the old content backup or Obsidian-specific dirs
content_backup/
_backups/
# attachments are needed for site rendering — must NOT be gitignored (Quartz glob respects gitignore)
# *_图片/

.vercel

.vercel/output/
