## Design Context

### Users

- **求知型访客（首要）**：带着问题或兴趣来到公开知识库，希望通过搜索、目录、主题、知识地图或学习路径，在两次主要操作内找到有价值的内容，并快速判断内容是否可信、适合自己以及下一步读什么。
- **潜在知识库建立者（次要）**：希望理解 wouldkeep 的方法、个人知识主权和公开边界，并获得可复用的结构与模板来建立自己的知识库。
- **回访读者与贡献者**：希望了解知识如何持续生长，并通过引用、分享、纠错或讨论参与完善，而不被迫加入社交网络。

核心任务是发现、阅读、判断、继续学习和负责任地分享知识。移动端、键盘操作、深色模式和 200% 缩放下必须保留完整核心任务。

### Brand Personality

wouldkeep 应当让人感到**温暖、安静、可信、求知、持续生长**。语言保持诚实、清晰、克制和有人味；不把未完成知识包装成权威结论，也不通过增长话术或社交压力驱动参与。

品牌关系为 `wouldkeep / 夔嵬`，主张是“拥有你的知识，也让知识照见他人”。产品是公开知识花园与可复用方法，不是多人知识库 SaaS。

### Aesthetic Direction

- 核心方向：**个人档案馆 × 公共知识网络**。
- 组合功能主义的秩序、编辑设计的长文层级，以及斯堪的纳维亚式自然色与柔和细节。
- 使用降低黄度的暖中性色、暖墨色正文、常青绿主要行动、知识蓝链接和陶土色档案状态；同时支持经过独立校准的深色模式。
- `Noto Serif SC` 只承担品牌与关键标题，`Noto Sans SC` 用于正文、导航、元数据和工具，`JetBrains Mono` 用于代码。
- 依靠排版、留白、细线、编号、元数据和关系连线表达结构；圆角克制，阴影只用于真正的层级变化。
- 避免通用博客模板、Notion 仪表盘、企业 SaaS 控制台、赛博朋克图谱、满屏玻璃卡片、过度 emoji、仿古纸张和算法信息流。

### Implementation Defaults

- 现有技术栈优先：Quartz 4.5.2、Preact 10、TypeScript、SCSS、静态生成与渐进式 SPA 导航。
- 扩展 Quartz 自有 `QuartzComponent` 组件体系和现有布局配置，不引入 Tailwind、Material UI、Chakra、shadcn、Radix/Base UI 或第二套 UI 架构。
- 设计系统以 `quartz/styles/tokens.scss` 的语义令牌为准；保留现有本地字体与 800px / 1200px Quartz 布局断点，新增响应式行为优先窄屏和内容驱动。
- 项目没有独立表单库、表格/数据网格库或虚拟列表库；后续仅在真实功能需要时评估，不为常规界面预装。
- 使用原生 HTML 语义承担可访问行为；ARIA 只补足原生语义无法表达的状态。
- 保持静态优先、低脚本成本、SPA 渐进增强、Markdown 内容工作流、现有 URL 兼容与本地字体加载。

### Design Principles

1. **让知识价值先于站点说明。** 每个页面先帮助用户理解和继续探索；不让全局工具、装饰或产品宣传压过正文。
2. **用层级和结构建立信任。** 先用语义、排版、间距与分组解决问题，再使用颜色和动效；不以卡片堆叠或视觉噱头掩盖信息结构。
3. **诚实呈现知识的生长状态。** 明确成熟度、来源、修订和许可；不把缺失信息或推断结果伪装成确定事实。
4. **使用熟悉、可达、可恢复的交互。** 优先原生控件、清晰焦点、44px 目标、键盘与屏幕阅读器等价路径；不依赖 hover、颜色或运动传递关键含义。
5. **为长期拥有与分享而构建。** 优先稳定 URL、静态输出、渐进迁移、低性能成本和可维护令牌；不以一次性炫技牺牲内容耐久性或个人知识主权。
