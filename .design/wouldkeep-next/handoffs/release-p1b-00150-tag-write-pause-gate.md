# Handoff: `release/p1b-00150-tag-write-pause-gate`

- Role: production database safety implementation
- Model / reasoning effort: Codex, inherited reasoning effort
- Worktree: `C:/Users/23012/Documents/Codex/2026-07-16/c-users-23012-desktop-wouldkeep/worktrees-next/release-p1b-00150-tag-write-pause-gate`
- Branch: `release/p1b-00150-tag-write-pause-gate`
- Baseline SHA: `4a813b5a268d5df63cff42ac7598b8c495bc1b9a`
- Current approved production-run SHA: `6caa8c4ce70e2c58aa9dbe3129174ad7b77ca746`
- Demonstrable slice: a versioned, fail-closed, reversible hard pause for `public.tags` and `public.document_tags` plus a transaction-local, owner-only deployment permit specific to `20260722000150`
- Approved research brief (or why none is needed): no external research; the work follows the explicit database/catalog contract and repository runbook

## Completed

- Added a short-transaction enable operation that takes fixed-order `SHARE ROW EXCLUSIVE NOWAIT` locks, proves both relations share the operator owner, requires every gate object to be absent, and installs one dedicated maintenance schema/function plus two exact ALWAYS statement triggers.
- Added an exact disable operation that locks in the same order, validates schema/function/table ownership, OIDs, function body/configuration, ACLs, comments, schema contents, trigger shape (`tgtype=62`, `tgenabled=A`), and catalog dependencies before dropping anything. Any validation or drop failure rolls the transaction back and leaves the pause active.
- Added a read-only state query. It rejects wrong-operator, partial, or drifted state and emits only exact gate OIDs/state plus SHA-256 fingerprints of the protected relations' catalog, ACL/RLS policies, and non-gate triggers.
- Added a fail-closed PostgreSQL 17 boundary for logical replication: enable and every state checkpoint reject an enabled inbound subscription relation on either target. Outbound Supabase Realtime publication is unaffected; physical/logical apply and restore are explicitly out of scope.
- Added a production-safe behavior probe covering operator, `anon`, `authenticated`, and `service_role`: zero-row `INSERT`/`UPDATE`/`DELETE` against both relations and no production `TRUNCATE`. It first derives each role's exact table/column ACL baseline; authorized statements must receive the gate's exact `55000`/stable message, while already unauthorized statements must remain blocked by `42501`. All 24 statements must be blocked and at least the operator's six statements must prove the gate path.
- Added an opt-in disposable PostgreSQL 17 harness and fixed SQL fixtures. It rejects URL query/fragment overrides, accepts only a literal loopback IP plus a prefixed throwaway database name, and independently verifies the server-side `inet_server_addr()` is non-null loopback before any write.
- Added a content-addressed sealed host orchestrator for the missing real-PG17 acceptance evidence. It pins the Supabase CLI binary hash and immutable database/auth/storage images, verifies all six exact digest images already exist in the same local Docker engine without registry access, maps each CLI-exact fully qualified `public.ecr.aws/supabase/...` mutable image alias to its reviewed digest, and exactly removes or restores each alias in cleanup. It disables seed and Realtime, requires auth/storage one-shot completion with zero helper residue, and deterministically renders one pure-SQL `roles.sql`: exactly one reviewed `\set` is removed and exactly one `\ir schema.sql` is replaced with the byte-identical pinned schema before the sole synthetic owner. Any other psql meta-command is rejected, the rendered/source hashes and zero meta-command/ledger-mutation contract are evidenced, and the bootstrap project contains exactly 19 pre-`00150` migrations with no standalone schema or ledger repair. A read-only attestation limits the initialized database owner to `postgres` or `supabase_admin` and requires `supabase_admin` to be a login-capable superuser; only the fixed rename/sanitization stages and the disposable replica-role probe use that exact local identity, while the business schema remains owned and verified by `postgres`. Login passwords and secret settings are removed, and clean shutdown is required before sealing. An offline physical archive is then hashed from a read-only source volume and restored into a fresh nonce volume while preserving the clean state and system identifier.
- Added a two-container no-egress acceptance topology: the final database uses deterministic secret-free configuration, `network=none`, no ports, read-only rootfs, exact tmpfs, and only the fresh PGDATA volume; an unprivileged read-only runner shares that exact network namespace, has no external mount/socket/credential, exact tmpfs only, no non-loopback interface or route, and no external DNS. It contains direct immutable PG17.6 clients plus PowerShell 7.5. Both input and output hashes are verified before and after the unchanged matrix.
- Added a dedicated no-network PGDATA quiesce helper for the Docker/Supabase bootstrap edge case where the wrapper exits while `pg_ctl -m fast` is still waiting. The helper is unprivileged, capability-free, read-only-root, has one exact read-write PGDATA mount and no published port, accepts only the observed `in production` or already-clean `shut down` state, performs crash recovery when required, then stops PostgreSQL cleanly. A separate read-only archive helper must independently prove `pg_controldata=shut down` before the clean-shutdown claim is written.
- Replaced Docker Desktop's unreliable Windows `docker cp` behavior for tmpfs with bounded text-file transfer from the still-running runner. Every file is first proven to be a regular file with an exact byte size, transferred through strict UTF-8, constrained to 8 MiB per file and 64 MiB total, checked against an exact file/directory inventory, and then SHA-256 verified. Failures atomically publish their own `failure-evidence-sha256.txt`, so partial matrix and recovery evidence can be preserved without exposing raw native arguments or credentials.
- Moved privileged-home credential-path absence checks into the sealed image build, where `/root` and `/home/postgres` can be inspected without weakening the final user. Runtime checks now cover only the runner's empty tmpfs HOME, while the host still proves no credential environment, bind, volume, socket, external route, or DNS capability reaches the runner.
- Added an independently confirmed temporary Windows Firewall gate for the CLI bootstrap's wildcard-requested random port. Docker Desktop may expose the request as a Windows wildcard listener or narrow it to IPv4/IPv6 loopback; the script records both layers, accepts only those four safe address forms, and rejects any other local-interface binding. It requires elevation and the running firewall service, installs one exact nonce-named inbound block rule scoped to non-loopback remote ranges, retains and revalidates its `InstanceID`, proves loopback remains usable, then stops/removes related containers before deleting only a freshly revalidated exact PersistentStore InputObject and proving zero rule residue. Removal requires same-engine, exact-zero scoped runtime/listener, persistent identity, and filter proofs. An incomplete ownership/runtime/PersistentStore proof retains the rule, records credential-free `firewall-retained.txt` evidence when possible, and fails closed. ActiveStore projection or global-profile drift alone is recorded and rethrown only after the fully proven exact persistent object is removed and both stores are zero. Every cleanup step still runs: a sole cleanup exception is rethrown exactly, while a main failure plus cleanup failure or multiple cleanup failures produce an `AggregateException` ordered as main first and cleanup exceptions in step order. The script never changes a global firewall profile or policy.
- Added a bounded two-client concurrency proof: a background transaction holds a granted `RowExclusiveLock` on `public.tags`; the harness proves the enable operation exits exactly `3` with SQLSTATE `55P03`, then proves exact absent state and zero gate residue. Release now uses a separately reviewed loopback-only SQL fixture that selects exactly one matching backend by database, login, application name, client address, and granted relation lock, calls `pg_terminate_backend(..., 5000)`, reaps the client, and proves the backend absent before the normal enable path.
- Added disposable-only coverage for UPSERT, `MERGE`, `COPY FROM`, `TRUNCATE`, `SECURITY DEFINER`, foreign-key cascade, and `session_replication_role=replica`, plus comment-drift rejection/recovery. The drift sequence now directly proves the read-only state query fails closed with exact `55000`/`wouldkeep_tag_write_pause_object_drift` before proving disable also rejects the drift.
- Every `psql` subprocess has a bounded wait and process-tree termination path. The background writer also has a server-side 75-second `statement_timeout`, and the outer `finally` retains its handle until stop/reap succeeds and separately polls for backend absence.
- Every native CLI/Docker child now starts from a cleared environment and receives only an explicit reviewed base plus call-specific values. Native failures retain split stdout/stderr hashes, counts, truncation state, fixed diagnostic signals, and bounded redacted lines; the three fixed container SQL stages write separately labeled `docker-sql-failure-<label>.txt` evidence. Raw output, arguments, and credentials never enter the exception. An evidence-write failure is preserved as the inner exception without replacing native-failure metadata. A timed-out process that cannot be reaped blocks runtime-cleanup proof and deliberately retains the firewall and sealed working directory.
- Reworked section 2 of the `00150` runbook into `absent pre-state -> enable -> active state/behavior -> active checks around every backup/ledger/dry-run/activity/preflight/tag-state read -> finally disable -> released state exactly equals initial`.
- Changed the schema dump to `--schema public,wouldkeep_maintenance` and required the dump to contain the maintenance schema/function, both triggers, both `ENABLE ALWAYS` states, and the public tables. This is a self-contained backup input, not a turnkey restore sequence: restoring it first would make the gate reject the following public-data load.
- Reopened deployment only through a dedicated continuous-pause channel. The owner-only helper requires exact locks on both relations and creates one transaction-local temporary permit; the trigger accepts it only for `public.tags` statement-level `UPDATE` in the same owner/backend/transaction and rejects forged temp tables, ordinary roles, security-definer indirection, all `document_tags` writes, and all other operations. The migration closes the permit before completion.
- Added a mandatory deployment postflight: exact 20/20 local/remote ledger through `00150`, zero pending migrations, canonical tag contract, immutable/reference fingerprints, exact-zero fixture residue, gate behavior still active, then byte-identical reviewed release. Mixed or unreadable state retains the pause. Ledger repair is never proactive.

## Changed files and scope

- Allowed paths changed:
  - `supabase/operations/20260722_tag_write_pause_enable.sql`
  - `supabase/operations/20260722_tag_write_pause_disable.sql`
  - `supabase/migrations/20260722000150_normalize_existing_tags_for_atomic_save.sql`
  - `supabase/tests/20260722_tag_write_pause_state.sql`
  - `supabase/tests/20260722_tag_write_pause_behavior.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable.ps1`
  - `supabase/tests/20260722_tag_write_pause_disposable_active_presence.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_baseline.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_cleanup.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_comment_drift.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_comment_restore.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_copy_from.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_deployment_permit.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_extended.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_residue.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_setup.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_writer.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_writer_absent.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_writer_release.sql`
  - `supabase/tests/20260722_tag_write_pause_disposable_writer_state.sql`
  - `supabase/tests/20260722_tag_write_pause_sealed.ps1`
  - `supabase/tests/20260722_tag_write_pause_sealed.Dockerfile`
  - `supabase/tests/20260722_tag_write_pause_sealed_db.Dockerfile`
  - `supabase/tests/20260722_tag_write_pause_sealed_manifest.sha256`
  - `supabase/tests/20260722_tag_write_pause_sealed_config.toml`
  - `supabase/tests/20260722_tag_write_pause_sealed_roles.sql`
  - `supabase/tests/20260722_tag_write_pause_sealed_bootstrap.sql`
  - `supabase/tests/20260722_tag_write_pause_sealed_rename.sql`
  - `supabase/tests/20260722_tag_write_pause_sealed_sanitize.sql`
  - `supabase/tests/20260722_tag_write_pause_sealed_attestation.sql`
  - `supabase/tests/20260722_tag_write_pause_sealed_container.ps1`
  - `supabase/tests/20260722_tag_write_pause_sealed_postgresql.conf`
  - `supabase/tests/20260722_tag_write_pause_sealed_pg_hba.conf`
  - `supabase/tests/20260722_tag_normalization_contract.sql`
  - `quartz/scripts/tagWritePauseGateGuard.test.ts`
  - `quartz/scripts/tagNormalizationMigrationGuard.test.ts`
  - `.design/wouldkeep-next/runbooks/20260722000150-tag-normalization.md`
  - `.design/wouldkeep-next/handoffs/release-p1b-00150-tag-write-pause-gate.md`
  - `.design/wouldkeep-next/DECISIONS.md`
- Non-authorized paths touched: none.
- Commander-owned hookup requested: independent database/catalog review and a real disposable PostgreSQL 17 matrix before any production application.

## Evidence

- Pre-manifest gate review: 24/24 non-manifest contracts passed, with the only full-suite failure exactly matching the intentionally frozen host-script hash.
- Frozen gate guard after the one-line manifest refresh: 25/25 passed.
- Current normalization migration guard: 7/7 passed.
- Fresh PowerShell parser-only validation of the host, disposable harness, and sealed container runner: passed with zero AST errors.
- `check-supabase-migrations`: passed; operations remain outside `supabase/migrations/**`.
- TypeScript `tsc --noEmit`: passed.
- Targeted Prettier for the TypeScript guard, runbook, and handoff: passed.
- `git diff --check`: passed after the final documentation update.
- UI evidence: not applicable; no UI or business code changed.
- Security evidence: static contract covers operator/anonymous/authenticated/service-role, exact no-bypass triggers, literal-loopback targeting, bounded process cleanup, NOWAIT contention rollback, exact-zero residue, fail-closed drift handling, strict child-environment isolation, credential-safe native diagnostics, deterministic pure-SQL bootstrap rendering, exact CLI alias restoration, compound shutdown/recovery proof, and bounded manifest-backed tmpfs evidence transfer. The current frozen guard passes 25/25, the normalization guard passes 7/7, TypeScript passes, the migration list is consistent, and all 50 sealed-input hashes pass. Run11bb supplies successful end-to-end live PostgreSQL 17 acceptance evidence; a separate post-run, read-only P0/P1 assertion pass independently rechecked its hashes, exact inventory, before/after fingerprints, behavior counts, extended cases, drift handling, restored state, isolation, firewall release, and exact-zero cleanup.
- Preserved current live-run evidence: Run11ah through Run11ap established the quiesce/archive/restore/no-egress topology, bounded UTF-8 evidence transfer, failure-manifest transfer, credential isolation, and exact-zero cleanup. Run11aq localized the initial matrix failure to the early linked-mode check; Run11ar proved the failing stage markers; Run11as reached the contention matrix and exposed that killing `psql` did not immediately terminate the sleeping backend. Run11au proved the new exact backend-release fixture (`backend_terminated`, client exit `2`, first-poll absence) and reached the behavior probe. Run11ax proved offline local-image verification, exact writer release, the complete 24-statement dual-path behavior result (`gate_blocks=6`, `acl_blocks=18`), and the first five extended cases. Run11ba completed the entire database matrix, including the replica-role case, but its host rejected the final evidence inventory because the runner excluded every basename `completed-utc.txt` instead of only the root self-description; cleanup still passed exactly. The corrected runner hashes `matrix/completed-utc.txt`. Run11bb then passed the full sealed flow and transferred an exact 44-file set backed by 42 ordinal SHA-256 manifest entries: pre/post attestation, deterministic writer termination, all 24 behavior statements, all six extended cases, hash verification, exact inventory, and host completion passed. It left zero containers, project networks, data volumes, temporary firewall rules, runner image, or working directory. Run11bb evidence is preserved at `C:/Users/23012/Documents/Codex/2026-07-16/wouldkeep-p1b-sealed-run11bb-20260802T101428Z-51098984ef1d`; all evidence directories are immutable and never reused.
- Migration or Edge Function deployed to production: **No**.
- Production connection, backup, and read-only preflight performed: **Yes, only under the separately authorized 2026-08-02 scopes described below**. Migration deployment and ledger repair performed: **No**.
- Authorized production preflight attempt on `2026-08-02`: the initial absent state, active pause, 24-statement zero-row behavior probe, three non-empty backups, and byte-identical released/initial state were proven. The run stopped before migration-list/dry-run/activity/preflight because Supabase CLI `2.109.1` emits `CREATE SCHEMA IF NOT EXISTS` and `CREATE OR REPLACE FUNCTION/TRIGGER`, while the structural marker expressions accepted only the shorter spellings and used a word boundary that could not follow a quoted trigger name. The reviewed disable operation restored writes in `finally`; a separate read-only production query from the hash-pinned snapshot again proved exact `gate=absent` with all four gate OIDs zero. No migration or ledger mutation ran. The corrected expressions now match each of the seven required schema markers and all four data/ledger markers in the preserved backups. Raw evidence remains outside the repository under `C:/Users/23012/Documents/Codex/2026-07-16/wouldkeep-p1b-production-preflight-20260802T103843Z`.
- Authorized production preflight rerun on `2026-08-02`: the corrected schema checks passed; the gate, 24-statement behavior probe, three fresh backups with hashes, and exact 19-remote/20-local migration list all passed. The run stopped at `db push --dry-run` because the hash-pinned evidence copy contained only the `supabase.exe` shim and not its required co-located `supabase-go.exe`. The reviewed `finally` path again produced a byte-identical released/initial gate state; no migration, activity probe, data preflight, or ledger mutation ran. The follow-up fix pins both exact `2.109.1` binary hashes, copies them under their canonical co-located names, sets `SUPABASE_GO_BINARY` to the pinned companion, and re-hashes both before every linked command. Raw evidence remains outside the repository under `C:/Users/23012/Documents/Codex/2026-07-16/wouldkeep-p1b-production-preflight-rerun-20260802T105117Z`.
- Third authorized production preflight run on `2026-08-02`: the pinned CLI pair worked; the gate, 24-statement behavior probe, three fresh backups with hashes, exact migration list, dry-run naming only `00150`, and activity gate all passed. The SQL preflight `DO` block exited zero, proving its aggregate assertions, but `supabase db query` suppressed its success-only `RAISE NOTICE`, so the outer sentinel check stopped the run before state capture. The reviewed `finally` path again produced a byte-identical released/initial gate state; no migration or ledger mutation ran. The follow-up adds one explicit, aggregate-only `SELECT` result row after the read-only assertion block; any assertion error aborts before that row. Raw evidence remains outside the repository under `C:/Users/23012/Documents/Codex/2026-07-16/wouldkeep-p1b-production-preflight-rerun2-20260802T110858Z`.
- Fourth authorized production preflight run on `2026-08-02`: exact approved commit `6caa8c4ce70e2c58aa9dbe3129174ad7b77ca746` completed the whole bounded window successfully. The initial gate was absent with all four gate OIDs zero; the active gate passed all 20 before/after checkpoints, the four-role/24-statement behavior probe, the activity gate, three fresh non-empty backups with independently matching SHA-256 hashes, the exact 19-remote/20-local migration list, and a dry-run naming only `20260722000150_normalize_existing_tags_for_atomic_save.sql` while explicitly stating no migration would be pushed. The explicit aggregate-only preflight row returned `tags=462`, `candidates=6`, `affected_references=65`, and `collisions=0`; the state fingerprint independently returned the same aggregate counts and distinct current/projected hashes. The reviewed `finally` path restored the exact absent baseline: released and initial state files are byte-identical, all four gate OIDs are zero, and no failure evidence exists. No migration or ledger mutation ran. Raw evidence remains outside the repository under `C:/Users/23012/Documents/Codex/2026-07-16/wouldkeep-p1b-production-preflight-rerun3-20260802T112332Z/p1b-tag-normalization-20260802T112500Z`; user-data backup contents were not copied into the repository or displayed.
- Dedicated-permit sealed PG17 acceptance on `2026-08-02`: the first live attempt exposed and transactionally rolled back a PostgreSQL reserved-word alias in the new trigger body; exact absent state, zero fixture residue, and exact-zero Docker/firewall cleanup passed. The second reached the permit matrix and showed the spoof test needed temporary `SELECT` as well as `UPDATE` to reach the statement trigger instead of failing earlier on column privilege; it also restored exact absent state and zero runtime/firewall residue. A third attempt stopped at the pre-matrix clean-shutdown stage when `pg_ctl` could not stop within 30 seconds; exact runtime/firewall cleanup again passed. The final fresh run completed the full sealed flow at `C:/Users/23012/Documents/Codex/2026-07-16/wouldkeep-p1b-sealed-permit-20260802T142957Z`: `tag_write_pause_disposable_matrix_passed`, `tag_write_pause_disposable_deployment_permit_passed`, 24/24 blocked statements (`gate_blocks=6`, `acl_blocks=18`), identical PG17/19-ledger before/after attestations, exact absent released state, zero fixture residue, exact 45-file/manifest-backed evidence transfer, and zero containers, networks, volumes, listeners, firewall rules, runner image, or working-directory residue. An external diagnostic wrapper reported failure only after completion because strict mode read an unset `$LASTEXITCODE`; the host's own completion, matrix, attestation, transfer, firewall-release, runtime-reconciliation, cleanup, and timestamps all passed.

## Decisions and contracts

- Decision entries affected: appended D-011 for the continuous pause plus transaction-local `00150` permit; no product-facing contract changed.
- Contract changes requested: deployment is open only through the exact dedicated channel and mandatory postflight; it is not a reusable bypass.
- Types, fixtures, and tests synchronized: operation SQL, migration permit hooks, read-only state/behavior/postflight probes, disposable harness/fixture files, static guards, runbook, decision log, and handoff are synchronized. No package, lockfile, or application file changed.

## Risk and recovery

- Known risks: Run11bb closes the live PostgreSQL 17 P0/P1 acceptance gap for bootstrap, compound shutdown/recovery, read-only physical archive/restore, final no-egress topology, exact writer contention/release, all 24 role/ACL behavior statements, all six extended cases, evidence transfer, and exact-zero cleanup; the separate post-run evidence review also passed. The CLI bootstrap briefly needs a wildcard-requested random host port protected by the exact temporary non-loopback Windows Firewall block; Docker Desktop may narrow the observed host listener to loopback. The final direct runner then shares only the database container's `network=none` namespace so both client and server see loopback without publishing a host port. The live contention test still holds `public.tags`, proving failure on the first ordered NOWAIT lock; contention on the second `public.document_tags` lock remains a P2 coverage gap, although transaction rollback and lock order are statically guarded.
- Rollback or forward-fix path: before enable, stop. While active on the same database, use only the reviewed disable operation; its all-or-nothing transaction preserves the pause on drift/failure. If a session is interrupted or state cannot be proven, treat writes as paused and stop. If `firewall-retained.txt` exists, preserve all evidence and never use a name-only removal. A later firewall removal needs separate authorization and must re-prove recorded-engine identity, zero nonce/project runtime and listeners, the full PersistentStore identity/filters, exact InputObject removal, and both-store zero; an unrecorded partial creation is manual-review-only. Disaster recovery needs a separate artifact that stops external writes, defines the gate-removal/data-load order, and uses semantic validation rather than comparing source and restored object OIDs.
- Blockers: the dedicated permit design and live PG17 acceptance are complete. Full repository verification, PR update/review/merge, and a new post-merge production evidence window remain before deployment. The production ledger is currently healthy and must not be repaired proactively.
- Next task prerequisites: finish full verification, push the reviewed commits, update and merge PR #40 under the site owner's authorization, then use a fresh isolated exact-20-migration snapshot for production backup, preflight, only-`00150` push, postflight, and reviewed gate release. `00200` remains absent from that deployment snapshot.
